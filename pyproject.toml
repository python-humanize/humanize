[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>=42",
  "setuptools_scm[toml]>=3.4",
]

[tool.black]
target_version = ["py37"]

[tool.setuptools_scm]
local_scheme = "no-local-version"
